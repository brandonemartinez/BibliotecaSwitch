@model Estudiante

@{
    ViewData["Title"] = "FindLibro";
}

<h1>Buscar prestamo de estudiantes</h1>

<h4>Estudiante</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="PrestamosActualesEstudiante">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NumeroEstudiante" class="control-label"></label>
                <input asp-for="NumeroEstudiante" class="form-control" />
                <span asp-validation-for="NumeroEstudiante" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Buscar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="PrestamosActuales">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
